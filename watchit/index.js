// console.log("code step by step") // it is a global module 

//built in module 
// const fs = require('fs')
// fs.writeFileSync("hello.txt","Hello");

// creating a server
// const http = require('http');
// http.createServer((req,resp) =>{
//     resp.write("hello this is yogi");
//     resp.end();
// }).listen(4500);

// package.json -- it consist of all coding realted detail of our project.
// package-lock.json -- it consist all package related detail.
const colors = require("colors");
console.log("hello".red)